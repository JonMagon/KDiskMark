name: kdiskmark
version: '2.0.0'
summary: A simple open-source disk benchmark tool for Linux distros
description: |
  This is my-snap's description. You have a paragraph or two to tell the
  most important story about your snap. Keep it under 100 words though,
  we live in tweetspace and your description wants to look good in the snap
  store.
license: GPL-3.0
base: core18

grade: devel # must be 'stable' to release into candidate/stable channels
confinement: devmode # use 'strict' once you have the right plugs and slots

parts:
  kdiskmark:
    plugin: cmake
    configflags:
      - -DCMAKE_BUILD_TYPE=Release
      - -DCMAKE_INSTALL_PREFIX=/usr
    build-packages:
      - g++
      - build-essential
      - dpkg-dev
      - qtbase5-dev
      - qttools5-dev
      - qttools5-dev-tools # Qt5LinguistTools
      - extra-cmake-modules
    stage-packages:
      - libqt5gui5
      - libqt5widgets5
      - libqt5core5a
      - fio
    organize:
      usr/share/qt5/translations/*.qm: usr/share/kdiskmark/translations/
    source: https://github.com/JonMagon/KDiskMark/archive/2.0.0.tar.gz
